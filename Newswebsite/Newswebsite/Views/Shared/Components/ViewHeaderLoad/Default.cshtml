@model List<News.Base.Models.News>
@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
        <li>
            <a href="/News/NewDetail?Id=@item.Id">
                @item.Title
            </a>
        </li>
    }
}
